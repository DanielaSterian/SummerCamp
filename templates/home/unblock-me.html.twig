{% extends 'base.html.twig' %}

{% block title %}Block someone{% endblock %}

{% block body %}

    <h1 class="card-title py-5 text-center"> Unblock me </h1>

    <div class="container">
        {{ form_start(form) }}

        <label>
            <b>Blocker</b>
        </label>
        {{ form_widget(form.blocker, {'attr': {'class': 'form-control'}}) }}

        <label>
            <b>Blockee</b>
        </label>
        {{ form_widget(form.blockee, {'attr': {'class': 'form-control'}}) }}

        <button class="btn btn-lg btn-primary" type="submit">
            Submit
        </button>
        {{ form_end(form) }}

    </div>

{% endblock %}